/// Material 3 window-size-class breakpoints, used to switch between
/// compact/medium/expanded layouts consistently across the app.
class AppBreakpoints {
  const AppBreakpoints._();

  static const double compact = 600;
  static const double medium = 1024;
}
